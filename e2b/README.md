# E2B Template Files

This directory isolates E2B template-related environment and scripts from the app code.

The actual template definition lives in `packages/template-builder/`.
See that package's README for build instructions.
