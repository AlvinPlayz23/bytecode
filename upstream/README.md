# Upstream References

Clone the MCP repos here for reference:

```bash
git clone https://github.com/OGMatrix/mcmodding-mcp.git upstream/mcmodding-mcp
git clone https://github.com/MCDxAI/minecraft-dev-mcp.git upstream/minecraft-dev-mcp
```

These repos are implementation references to absorb, not product surfaces to expose.

From mcmodding-mcp, absorb:
- doc indexing ideas
- concept/example retrieval
- ranking/chunking strategy

From minecraft-dev-mcp, absorb:
- symbol/source lookup ideas
- decompile/source access patterns
- mixin validation logic
- version-aware source analysis
